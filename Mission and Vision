{/* Mission & Vision Section */}
<section className="bg-gradient-to-r from-black via-gray-900 to-black rounded-3xl shadow-lg p-8 max-w-6xl mx-auto py-16 px-6 mt-12 text-white">

  {/* Our Mission */}
  <motion.div
    initial={{ opacity: 0, y: 30 }}
    whileInView={{ opacity: 1, y: 0 }}
    transition={{ duration: 1, delay: 0 }}
    viewport={{ once: true }}
    className="flex flex-col md:flex-row items-center gap-8 mb-12"
  >
    <div className="flex-1">
      <h3 className="text-2xl font-semibold mb-4" style={{ color: "#855C2D" }}>
        Our Mission
      </h3>
      <p className="text-gray-200 leading-relaxed text-base sm:text-lg">
        TNS Tours provides safe, comfortable, and personalized travel experiences across Rwanda.
        Our goal is to connect travelers with Rwanda’s breathtaking landscapes, wildlife,
        and rich culture while ensuring every journey is memorable and enriching.
      </p>
    </div>

    <div className="flex-1">
      <img
        src="/mission.jpg"
        alt="Our Mission"
        className="rounded-2xl shadow-md object-cover w-full h-72 cursor-pointer transform transition-transform duration-300 hover:scale-105"
        onClick={(e) => {
          const img = e.currentTarget;
          img.style.transition = "transform 0.3s";
          img.style.transform = img.style.transform.includes("scale(1.5)") 
            ? "scale(1)" 
            : "scale(1.5)";
        }}
      />
    </div>
  </motion.div>

  {/* Our Vision */}
  <motion.div
    initial={{ opacity: 0, y: 30 }}
    whileInView={{ opacity: 1, y: 0 }}
    transition={{ duration: 1, delay: 1 }}
    viewport={{ once: true }}
    className="flex flex-col md:flex-row items-center gap-8"
  >
    <div className="flex-1 order-2 md:order-1">
      <img
        src="/vision.jpg"
        alt="Our Vision"
        className="rounded-2xl shadow-md object-cover w-full h-72 cursor-pointer transform transition-transform duration-300 hover:scale-105"
        onClick={(e) => {
          const img = e.currentTarget;
          img.style.transition = "transform 0.3s";
          img.style.transform = img.style.transform.includes("scale(1.5)") 
            ? "scale(1)" 
            : "scale(1.5)";
        }}
      />
    </div>

    <div className="flex-1 order-1 md:order-2">
      <h3 className="text-2xl font-semibold mb-4" style={{ color: "#855C2D" }}>
        Our Vision
      </h3>
      <p className="text-gray-200 leading-relaxed text-base sm:text-lg">
        To showcase Rwanda as a premier travel destination, offering unforgettable adventures and authentic cultural experiences.
        Through sustainable and high-quality tourism, we aspire to leave a positive impact on both travelers and local communities.
      </p>
    </div>
  </motion.div>

</section>

